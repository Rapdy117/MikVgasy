## Optimiser une requête lente

Mémoire projet:
- Lire `docs/project_rules.md` avant toute modification.
- Lire/mettre à jour `docs/DATA_MODEL.md` si la correction ajoute ou modifie index, tables, champs, relations ou ownership de données.
- Lire/mettre à jour `docs/DECISIONS.md`, `docs/ARCHITECTURE.md` ou `docs/KNOWN_ISSUES.md` seulement si l'optimisation change une décision, un flux ou l'état d'un problème connu.

Fichier: [À remplir]
Temps actuel: [ex: 1.2s]
Temps cible: [ex: <200ms]
Table principale: [ex: live_metrics]

Mesurer d'abord:
  time php api/fichier.php

Cause probable:
- Index manquant WHERE/JOIN
- Boucle N+1
- SELECT * (trop colonnes)
- Pas de cache

Analyser + proposer fix optimisé + benchmark avant/après.

Mode: Agent (modif + test)
Effort: medium
